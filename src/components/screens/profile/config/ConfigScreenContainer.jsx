import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ConfigScreen from "./ConfigScreen";

const ConfigScreenContainer = () => {
  return <ConfigScreen />;
};

export default ConfigScreenContainer;

const styles = StyleSheet.create({});
